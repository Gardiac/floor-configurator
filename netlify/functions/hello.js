export const handler = async () => ({ statusCode: 200, headers: CORS(), body: JSON.stringify({ ok: true }) });
function CORS(){ return { "Access-Control-Allow-Origin":"*", "Content-Type":"application/json" }; }